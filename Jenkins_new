pipeline{

agent any

environment{
 SPECIFIC_CAUSE = currentBuild.getBuildCauses('hudson.model.Cause$UserIdCause')
 User_id = "${SPECIFIC_CAUSE.userId}"
}

  stages{
 stage("from l0 to l1") {
 
 when{
environment name: "User_id" , value: "[ravi]"
}
    steps{
    //script{
    //def specificCause = currentBuild.getBuildCauses('hudson.model.Cause$UserIdCause')
//if (specificCause) {
    //println("Executed by user $specificCause.userName")
   
    //echo "specificCause: $specificCause.userName"
    //echo "specificCause: ${specificCause.userId}"
    //def user_id = ${specificCause.userId}
//}

echo"Ravi stillllll doneeeeeeee..."
//echo "specificCause: $SPECIFIC_CAUSE.userId"
echo"Ravi u doneeeeeeee..."
echo "${User_id}"

    //}
    
    }
           
        }
        
}
  
  }
